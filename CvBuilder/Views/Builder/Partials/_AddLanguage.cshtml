@model CvBuilder.Models.LanguageSkills
@{ string hide = ""; }
<div>
    @Html.ValidationSummary(true, "", new { @class = "text-danger" })

    @for (int i = 0; i < Model.LanguageItems.Count; i++)
    {
        hide = (i == 0) ? "" : "display:none";
        <div class="lang-item" style="@hide">
            <div class="row">
                <div class="form-group col-lg-6">
                    @Html.LabelFor(model => Model.LanguageItems[i].Language, "Language", null)
                    @Html.DropDownListFor(model => Model.LanguageItems[i].Language, new SelectList(Model.AllLanguages), new { @class = "form-control" })
                </div>
                <div class="form-group col-lg-6">
                    @Html.LabelFor(model => Model.LanguageItems[i].Level, "Level", null)
                    @Html.DropDownListFor(model => Model.LanguageItems[i].Level, new SelectList(Model.AllLevels), new { @class = "form-control" })
                </div>
            </div>

            <div style="float:right;">
                <input type="button" style="display:inline" value="Save" class="btn btn-success save-section" />
            </div>
        </div>
    }
</div>

